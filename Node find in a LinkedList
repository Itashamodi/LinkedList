public class Solution {


	public static int findNodeRec(LinkedListNode<Integer> head, int n) 
    {
        int index=0;
    	while(head!=null)
        {
            if(head.data==n)
            {
                return index;
            }
            index++;
            head=head.next;
        }
        return -1;
	}

}
